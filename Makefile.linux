
LINKER_OBJ2EXE=gcc
LINKER_OBJ2EXE_FLAGS=
LINKER_OBJ2EXE_OUTPUT=-o
PREREQUISITES_ALL=$^
EXECUTABLE_SUFFIX=.out
OBJECT_SUFFIX=.o
REMOVE_FILE=rm -f
DIRECTORY_SEPARATOR=/

%$(OBJECT_SUFFIX):%.c
	gcc -Wall -c -o$@ $^

include Makefile.inc

